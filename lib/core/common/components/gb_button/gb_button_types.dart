enum GbButtonSize { sm, md, lg, xl, xxl }

enum GbButtonHierarchy {
  primary,
  secondaryGray,
  secondaryColor,
  tertiaryGray,
  tertiaryColor,
  linkGray,
  linkColor,
}

enum GbButtonState { normal, pressed, disabled, loading }
