enum GbInputSize { sm, md }

enum GbInputType {
  text,
  iconLeading,
  leadingDropdown,
  trailingDropdown,
  leadingText,
  paymentInput,
  tags,
  trailingButton,
  password,
  passwordIconLeading,
  count,
}

enum GbInputState { normal, filled, active, disabled }
